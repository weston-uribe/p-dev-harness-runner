# V0.3.0 — Release contract

**Tag status:** Tagged — annotated tag `v0.3.0` on `f55f21825802a246a9db130ae5e128ad8165384d`. GitHub release: https://github.com/weston-uribe/agentic-product-development-harness/releases/tag/v0.3.0

**npm status:** Published — `p-dev-harness@0.3.0` on https://www.npmjs.com/package/p-dev-harness

**Release type:** GitHub **source release** (annotated tag + curated release notes) plus public npm CLI package **`p-dev-harness@0.3.0`**.

**npm naming note:** Registry rejected bare `p-dev` as too similar to existing [`pdev`](https://www.npmjs.com/package/pdev). The published package name is `p-dev-harness`.

## TL;DR

V0.3.0 is the guided-onboarding and distribution release. Product managers can start with `npx --yes p-dev-harness@0.3.0` without cloning the source repository. The seven-step Configure GUI automates service credentials, Linear workspace setup, Vercel webhook bridge, target repos, cloud secrets, and guarded workflow install PR finalization.

## Who this is for

- Product managers and solo operators who want guided harness onboarding
- Contributors who develop the harness from the source repository
- Teams already on Linear + GitHub + GitHub Actions + Vercel + Cursor

## Primary install command

```bash
npx --yes p-dev-harness@0.3.0
```

Requires Node.js 22+. macOS is the validated packaged platform; use `--no-open` elsewhere.

Full guide: [`docs/p-dev.md`](../p-dev.md)

## Architecture / workspace model

| Boundary | Role |
|----------|------|
| `p-dev-harness` npm package | Packaged operator shell and Configure GUI launcher |
| Operator workspace (`~/.p-dev`, `P_DEV_HOME`, `--workspace`) | Durable local secrets and config |
| Public template (`weston-uribe/p-dev-harness-template`) | Provisioning source for private harness workspace |
| Private `OWNER/p-dev-harness` | Managed harness workspace for cloud automation |
| Target repos | Product codebases the harness operates on |

## What ships

| Area | V0.3.0 |
|------|--------|
| Product install | `p-dev-harness@0.3.0` on public npm |
| Configure GUI | Seven-step guided setup with confirmation gates |
| Packaged provisioning | Private harness workspace from public template |
| Linear setup | Automated workspace/status configuration |
| Vercel bridge | Env vars, webhook, redeploy polling, signed verification |
| Cloud secrets | Harness repo GitHub Actions secret writes |
| Target workflow | Install PR create/reuse, checks, guarded merge, production verify |
| Canonical skills | Six skills under `.agents/skills/` |
| Source release | Annotated tag + GitHub release on this repository |

## Setup flow

1. Connect services (optional packaged harness workspace provisioning)
2. Set up Linear workspace
3. Set up Vercel webhook bridge
4. Choose target repo(s)
5. Check local readiness
6. Connect cloud secrets
7. Install target repo workflow (guarded auto-finalization for system-owned setup PRs)

Remote mutations require explicit confirmation. Ordinary product PRs remain governed by Linear status gates.

## Canonical skills

Implemented under `.agents/skills/`:

- `issue-intake`
- `code-health-audit`
- `architecture-evolution-audit`
- `security-audit`
- `planner`
- `implementation`

Deferred: `performance-cost-audit`, skill registry/package manager, manifests, provider adapters, runner-skill prompt integration. Planner and implementation SKILL.md files do not replace `src/prompts/*.md` runner implementation.

## Changes from v0.2.0

| v0.2.0 | v0.3.0 |
|--------|--------|
| Source clone only | `p-dev-harness` npm package for product onboarding |
| Manual/scattered setup docs | Seven-step guided Configure GUI |
| No canonical skills | Six canonical skills documented and implemented |
| No public template provisioning | Private harness workspace from public template |
| Manual workflow install guidance | Guarded Step 7 auto-finalization for setup PRs |
| Source release only | Source release + public npm package |

## Security model

- Secrets in local workspace, GitHub Actions secrets, and Vercel env vars — never in the published package
- Confirmation-gated remote writes with fingerprint validation
- Output redaction before logs and artifacts
- Published tarball excludes credentials and operator state

Details: [`docs/security.md`](../security.md)

## Validation evidence

- Codespaces source-based Configure GUI validation
- Controlled separate-account packaged onboarding through Setup complete
- Package pack/inspect tests and tarball smoke (Configure route HTTP 200)
- Registry-installed smoke: `npx --yes p-dev-harness@0.3.0 --no-open` → `/settings/configure` HTTP 200
- Expanded setup, GUI, and provisioning test suite

**Not validated:** full real issue lifecycle from an isolated npm-installed workspace.

## Support matrix

| Subsystem | V0.3.0 |
|-----------|--------|
| Product install | `p-dev-harness@0.3.0` (npm) |
| Packaged platform | macOS validated; `--no-open` elsewhere |
| Node.js | 22+ |
| Agent provider | Cursor Cloud Agents |
| Product system | Linear |
| SCM / PR | GitHub |
| Runner | GitHub Actions |
| Preview / bridge | Vercel |

## Known limitations

- Cursor-only agent provider
- Linear / GitHub / GitHub Actions / Vercel stack
- macOS validated for packaged browser auto-launch
- No full isolated npm issue lifecycle validated
- No automatic private harness workspace upgrade/sync
- Public template compatibility dependency
- Not production SaaS or provider-agnostic
- Manual eval rubrics where automation is not implemented

## Upgrade notes from v0.2.0

- Product managers: use `npx --yes p-dev-harness@0.3.0` instead of cloning for onboarding
- Contributors: continue cloning source repository
- Existing source-based workspaces: unchanged; operator workspace remains under `~/.p-dev` by default
- Template main updated for authoritative provenance after npm package rename

## Template dependency / provenance

| Artifact | SHA / value | Notes |
|----------|-------------|-------|
| Template repo | `weston-uribe/p-dev-harness-template` | Public GitHub template |
| Template **main** (authoritative) | `9cdf5534638194036b55d84a3d959f092c47c201` | PR [#17](https://github.com/weston-uribe/p-dev-harness-template/pull/17) — aligned to primary `f55f218`, `p-dev-harness` naming |
| Template tag `v0.3.0` (immutable) | `8dac15dfeef798909f9216028708c66aebd55dd6` | PR [#16](https://github.com/weston-uribe/p-dev-harness-template/pull/16) — initial sync from primary `156eab6`; **not moved** |
| `templateContentId` on template main | `agentic-product-development-harness-f55f21825802a246a9db130ae5e128ad8165384d-curated-v1` | Matches authoritative primary release commit |

GitHub template provisioning uses repository **default branch** (`main`), so corrected template `main` is authoritative for new private workspaces. The immutable template tag `v0.3.0` remains a historical snapshot from the pre-rename sync.

## Immutable release evidence

| Field | Value |
|-------|-------|
| **RELEASE_SHA** | `f55f21825802a246a9db130ae5e128ad8165384d` |
| Primary annotated tag object | `e6cdc5f69c1f394e0403bc90a14b317e2b6b134b` |
| Primary tag peeled commit | `f55f21825802a246a9db130ae5e128ad8165384d` |
| GitHub release | https://github.com/weston-uribe/agentic-product-development-harness/releases/tag/v0.3.0 |
| Release-prep PR | https://github.com/weston-uribe/agentic-product-development-harness/pull/56 |
| npm rename PR | https://github.com/weston-uribe/agentic-product-development-harness/pull/57 |
| npm package | `p-dev-harness@0.3.0` |
| npm dist-tag `latest` | `0.3.0` |
| npm dist.shasum | `fbc2c9e3d29ad4181776008b1880e5845e3aec53` |
| npm dist.integrity | `sha512-0p2ggAnyVOv4QVEpFFPp54e2eaPfFNfyLhVdyYLySfTVym/iOcB6nJR0v3UcRTM9uQc05F2bayG9i7JltnuUmA==` |
| Local tarball | `packages/p-dev/p-dev-harness-0.3.0.tgz` |
| Local tarball bytes | 1,630,506 |
| Local tarball SHA-1 | `fbc2c9e3d29ad4181776008b1880e5845e3aec53` |
| Local tarball SHA-256 | `d7b99605f40f1837ef9a7bd64dd1f712d18259c6dafc9124527337b29a60c916` |
| Registry smoke | `npx --yes p-dev-harness@0.3.0 --no-open` → HTTP 200 on `/settings/configure` |
| Release timestamp (UTC) | 2026-07-13 |

## Release readiness checklist

- [x] Release-prep PR merged to `main`
- [x] npm package-name correction PR merged
- [x] Required checks green on merge commits
- [x] Template sync PR merged; immutable template tag `v0.3.0` at `8dac15d`
- [x] Template provenance correction merged to template `main`
- [x] `p-dev-harness@0.3.0` published from exact tarball
- [x] Registry smoke: `npx --yes p-dev-harness@0.3.0 --no-open`
- [x] Annotated primary tag `v0.3.0` pushed at `RELEASE_SHA`
- [x] GitHub release created
- [x] Finalization PR records immutable tag/npm/template evidence

## Tag / npm / GitHub release procedure

Historical operator steps: [`release-process.md`](release-process.md)
