import { SettingsPromptsClient } from "@/components/settings/settings-prompts-client";

export const dynamic = "force-dynamic";

export default function SettingsPromptsPage() {
  return <SettingsPromptsClient />;
}
